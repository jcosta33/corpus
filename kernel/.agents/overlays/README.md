# Overlays

> Project rule bundles (§26.6). Non-kernel, project-authored; cite/quote upstream nodes but are never cited by them. Populated per project; the stdlib ships this directory empty with this README.
